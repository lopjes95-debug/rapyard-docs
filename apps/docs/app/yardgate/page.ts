import React from "react";

export default function YardGatePage(): JSX.Element {
  return (
    <section className="p-8 space-y-4">
      <h1 className="text-3xl font-semibold">The RapYard Gate</h1>

      <p className="text-slate-300 text-sm">
        The Gate is the entry ritual into the Yard — session lobby, access control,
        and the queue that leads creators into battles, cyphers, and arenas.
      </p>

      <p className="text-slate-400 text-sm">
        Every user passes through the Gate. It’s the cinematic opener, the identity marker,
        and the routing layer that directs tenants, docs, and core experiences.
      </p>
    </section>
  );
}
