NODE_ENV=production

# Queue
WORKER_QUEUE_URL=redis://<redis-url>
WORKER_QUEUE_TOKEN=<redis-token>

# Supabase Service Role (server-only)
SUPABASE_SERVICE_ROLE=<service-role-key>

# Logging
WORKER_LOG_LEVEL=info
